/**
 * Single source of truth for the unified campsite fire scene key (Ink legacy name).
 * FireBuildingMinigame registers under this key — keep all scene.start/launch in sync.
 */
export const FIRE_CAMPSITE_SCENE_KEY = 'FireCampsiteMinigame'
